class LastFM_Auth {
  Future getMobileSession(String username, String password) {
    throw UnimplementedError();
  }

  Future getSession(String token) {
    throw UnimplementedError();
  }

  Future getToken() {
    throw UnimplementedError();
  }
}
